# Write a program that reads three integers and displays their sum on the screen. 
# Each number is written in a separate line.

# Input data format:
# The program is fed three integers, each on a separate line, as input.

# Output data format:
# The program should output the sum of the entered numbers.

a = int(input())
b = int(input())
c = int(input())
print(a + b + c)