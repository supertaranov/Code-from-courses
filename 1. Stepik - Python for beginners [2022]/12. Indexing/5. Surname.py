# The program is given three strings as input: first name, last name, and patronymic. 
# Write a program that outputs the initials of a person.

# Input data format:
# The program is given three strings as input, each on a separate line.

# Output data format:
# The program should output the person's full name.

# Note. It is guaranteed that first name, last name and patronymic begin with a capital letter.

a = input()
b = input()
c = input()
print(b[0] + a[0] + c[0])